import { SummaryItemProp } from "../../interface/props/summary-item-prop.interface";


export function SummaryItem({ reduce_
    class_name }: SummaryItemProp)
{
    return (
        <div className={`info-entry`}>

        </div>
    );
}